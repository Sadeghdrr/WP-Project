# =============================================================================
# WP-Project/frontend/.dockerignore
# Applied when Docker builds the frontend image (context: ./frontend)
# =============================================================================

# Node deps (installed fresh inside the image via npm ci)
node_modules/

# Build output
dist/
dist-ssr/
build/
.cache/

# Vite artefacts
*.local
vite.config.ts.timestamp-*

# TypeScript build info
*.tsbuildinfo

# Test / coverage
coverage/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Git
.git/
.gitignore

# Secrets — never bake .env into the image
.env
.env.*
!.env.example

# Docker files themselves
Dockerfile
.dockerignore

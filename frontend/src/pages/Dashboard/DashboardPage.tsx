import { PlaceholderPage } from "../../components/ui";

/**
 * Modular Dashboard placeholder.
 * Requirement (§5.3): Show role-appropriate modules (800 pts).
 */
export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Modular dashboard — each module (case review, complaint review, detective board, etc.) will appear based on your permissions."
    />
  );
}

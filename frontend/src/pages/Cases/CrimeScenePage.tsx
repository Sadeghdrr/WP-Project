import { PlaceholderPage } from "../../components/ui";

/**
 * Report a crime scene (case creation via crime scene).
 * Requirement (§4.2.2).
 */
export default function CrimeScenePage() {
  return (
    <PlaceholderPage
      title="Report Crime Scene"
      description="Police officer logs crime scene details, time/date, and witness information to create a new case."
    />
  );
}

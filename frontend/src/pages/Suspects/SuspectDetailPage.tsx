import { PlaceholderPage } from "../../components/ui";

/**
 * Suspect detail page.
 */
export default function SuspectDetailPage() {
  return (
    <PlaceholderPage
      title="Suspect Detail"
      description="View suspect profile, interrogation records, guilt scores, and verdict history."
    />
  );
}

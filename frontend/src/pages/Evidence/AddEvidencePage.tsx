import { PlaceholderPage } from "../../components/ui";

/**
 * Add new evidence to a case.
 * Requirement (§4.3).
 */
export default function AddEvidencePage() {
  return (
    <PlaceholderPage
      title="Add Evidence"
      description="Register new evidence for this case — select type (testimony, biological, vehicle, ID document, other) and fill details."
    />
  );
}

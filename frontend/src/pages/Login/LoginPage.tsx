import { PlaceholderPage } from "../../components/ui";

/**
 * Login page placeholder.
 * Requirement (§5.2): Dedicated login page.
 */
export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="Sign in to access the LAPD Case Management System. Username, email, phone, or national ID + password."
    />
  );
}

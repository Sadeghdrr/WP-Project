import { PlaceholderPage } from "../../components/ui";

/**
 * Detective Board placeholder.
 * Requirement (§5.4): Drag-drop board with connections + export (800 pts).
 */
export default function DetectiveBoardPage() {
  return (
    <PlaceholderPage
      title="Detective Board"
      description="Interactive drag-and-drop board — place documents and notes, connect them with red lines, and export as an image."
    />
  );
}

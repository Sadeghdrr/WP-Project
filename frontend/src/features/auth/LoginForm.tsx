import React from 'react';

// TODO: Login form component
// Fields: identifier (username/email/phone/national_id), password
// Submit → auth.api login() → AuthContext

export const LoginForm: React.FC = () => {
  return <form>{/* TODO: Implement Login Form */}</form>;
};

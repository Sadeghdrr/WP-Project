import React from 'react';

// TODO: Evidence list/table component with filters (type, case, date)
// Columns: title, type, case, registered_by, created_at, verification status

export const EvidenceTable: React.FC = () => {
  return <div>{/* TODO: Implement Evidence Table */}</div>;
};

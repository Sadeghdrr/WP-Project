import React from 'react';

// TODO: Case table/list component with filters (status, crime level, creation type)
// Columns: ID, title, crime level, status, assigned detective, created_at
// Row click → navigate to case detail

export const CaseTable: React.FC = () => {
  return <div>{/* TODO: Implement Case Table */}</div>;
};

import React from 'react';

// TODO: Layout for unauthenticated pages (Login, Register)
// Centered card layout, no sidebar, minimal chrome

export const AuthLayout: React.FC = () => {
  return (
    <div>
      {/* TODO: Centered auth container with <Outlet /> */}
    </div>
  );
};

import React from 'react';

// TODO: Reusable Input component (text, email, password, number, textarea)
// Props: label, name, type, value, onChange, error, placeholder, required

export const Input: React.FC = () => {
  return <input />;
};

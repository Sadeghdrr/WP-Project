import React from 'react';

// TODO: Reusable Button component with variants (primary, secondary, danger, ghost)
// Props: label, onClick, variant, size, disabled, loading, icon, type

export const Button: React.FC = () => {
  return <button>{/* TODO: Implement Button */}</button>;
};

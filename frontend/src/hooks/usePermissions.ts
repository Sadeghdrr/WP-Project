// TODO: Custom hook for checking RBAC permissions on the UI
// - hasPermission(permissionCodename: string): boolean
// - hasAnyPermission(codenames: string[]): boolean
// - hasAllPermissions(codenames: string[]): boolean
// Reads permissions from AuthContext (user's role permissions)

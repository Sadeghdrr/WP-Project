// TODO: Evidence API calls
// - GET    /api/evidence/                          → listEvidence(params)
// - POST   /api/evidence/                          → createEvidence(data)
// - GET    /api/evidence/:id/                      → getEvidenceDetail(id)
// - PATCH  /api/evidence/:id/                      → updateEvidence(id, data)
// - DELETE /api/evidence/:id/                      → deleteEvidence(id)
// - POST   /api/evidence/:id/verify/               → verifyEvidence(id, result) (Coroner)
// - POST   /api/evidence/:id/files/                → uploadEvidenceFile(id, file)
// - GET    /api/evidence/case/:caseId/             → listEvidenceByCase(caseId)

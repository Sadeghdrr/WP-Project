// TODO: Auth API calls
// - POST /api/accounts/login/          → login(credentials)
// - POST /api/accounts/register/       → register(data)
// - POST /api/accounts/token/refresh/  → refreshToken(refresh)
// - GET  /api/accounts/me/             → getCurrentUser()
// - POST /api/accounts/logout/         → logout()
